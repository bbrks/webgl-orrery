/**
 * @fileoverview Settings.js - Contains default settings
 * @author Ben Brooks (beb12@aber.ac.uk)
 * @version 1.0
 */

// Set up a global settings array to access settings
var settings = new Array();

// Set some defaults
settings['drawLighting'] = true;
settings['fov']          = 0.785; // Field of view (radians)
settings['zoom']         = -10; // Translate Z of viewMatrix
settings['simSpeed']     = 1;
